def find_needle_in_haystack(haystack, needle):
    for i in range(len(haystack) - len(needle) + 1):
        if haystack[i:i + len(needle)] == needle:
            return i
    return -1

# Example
haystack = "sadbutsad"
needle = "sad"
print(find_needle_in_haystack(haystack, needle))  # Output: 0
